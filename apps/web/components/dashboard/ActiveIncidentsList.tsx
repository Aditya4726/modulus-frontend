import { AlertTriangle, CheckCircle } from "lucide-react";
import type { DashboardMetrics } from "../../types/dashboard";
/** Renders the active incident summary list for the dashboard. */
export function ActiveIncidentsList({ incidents = [] }: { incidents?: DashboardMetrics["activeIncidents"] }) {
	return <section className="rounded-2xl border border-white/10 bg-[#13131d] p-5"><div className="flex justify-between"><h2 className="font-semibold text-white">Active Incidents</h2><span className="text-xs text-slate-600">—</span></div><div className="mt-4 space-y-4">{incidents?.length ? incidents.map((incident) => { const iconClassName = incident.severity === "critical" ? "text-rose-400" : incident.severity === "warning" ? "text-amber-400" : "text-emerald-400"; const Icon = incident.severity === "resolved" ? CheckCircle : AlertTriangle; return <div key={incident.title} className="flex gap-3"><span className={`mt-1 ${iconClassName}`}><Icon className="h-4 w-4" /></span><div className="min-w-0 flex-1"><div className="flex justify-between gap-2"><strong className="truncate text-sm text-slate-200">{incident.title}</strong><span className="text-[10px] text-slate-500">{incident.time}</span></div><p className="mt-1 truncate text-xs text-slate-500">{incident.subtitle}</p><p className="mt-1 text-[10px] text-slate-500">● {incident.status}</p></div></div>; }) : <p className="py-8 text-center text-sm text-slate-500">— Incident details not returned</p>}</div></section>;
}
"use client";
import { Area, AreaChart, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
/** Renders the dark reliability-over-time area chart and summary footer. */
export function ReliabilityChart({ trend = [], summary }: { trend?: Array<{ date: string; value: number }>; summary: { uptime: string; downtime: string; incidents: string; resolutions: string } }) {
	return <section className="rounded-2xl border border-white/10 bg-[#13131d] p-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold text-white">Reliability Over Time</h2><p className="mt-1 text-xs text-slate-500">Daily system reliability score</p></div><select className="rounded-md border border-white/10 bg-white/5 px-2 py-1 text-xs text-slate-300"><option>Backend data</option></select></div><div className="mt-5 h-64">{trend.length ? <ResponsiveContainer><AreaChart data={trend}><defs><linearGradient id="reliabilityFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0" stopColor="#2dd4bf" stopOpacity={.5} /><stop offset="1" stopColor="#2dd4bf" stopOpacity={0} /></linearGradient></defs><XAxis dataKey="date" tickFormatter={(value) => value.slice(5)} tickLine={false} axisLine={false} stroke="#64748b" /><YAxis tickLine={false} axisLine={false} stroke="#64748b" /><Tooltip contentStyle={{ background: "#1b1b28", border: "1px solid #353548", color: "#fff" }} formatter={(value) => [`${value}%`, "Reliability"]} /><Area type="monotone" dataKey="value" stroke="#2dd4bf" fill="url(#reliabilityFill)" strokeWidth={3} dot={{ fill: "#5eead4", r: 3 }} animationDuration={1200} /></AreaChart></ResponsiveContainer> : <div className="grid h-full place-items-center text-sm text-slate-500">— No reliability trend returned</div>}</div><div className="grid grid-cols-4 divide-x divide-white/10 border-t border-white/10 pt-4 text-center text-xs text-slate-500"><div>Uptime<strong className="mt-1 block text-sm text-white">{summary.uptime}</strong></div><div>Downtime<strong className="mt-1 block text-sm text-white">{summary.downtime}</strong></div><div>Incidents<strong className="mt-1 block text-sm text-white">{summary.incidents}</strong></div><div>Resolutions<strong className="mt-1 block text-sm text-white">{summary.resolutions}</strong></div></div></section>;
}
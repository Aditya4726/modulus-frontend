import type { DashboardMetrics } from "../../types/dashboard";
/** Renders agent health status rows and progress bars. */
export function AgentHealthList({ agents = [] }: { agents?: DashboardMetrics["agents"] }) {
	return <section className="rounded-2xl border border-white/10 bg-[#13131d] p-5"><div className="flex justify-between"><h2 className="font-semibold text-white">Agent Health</h2><span className="text-xs text-slate-600">—</span></div><div className="mt-4 space-y-4">{agents?.length ? agents.map((agent) => { const statusClassName = agent.status === "healthy" ? "text-emerald-400" : agent.status === "degraded" ? "text-amber-400" : "text-rose-400"; const barClassName = agent.status === "healthy" ? "bg-emerald-400" : agent.status === "degraded" ? "bg-amber-400" : "bg-rose-400"; return <div key={agent.name}><div className="flex items-center justify-between"><div><strong className="text-sm text-slate-200">{agent.name}</strong><p className="text-xs text-slate-500">{agent.framework}</p></div><span className={`text-xs ${statusClassName}`}>{agent.status}</span></div><div className="mt-2 h-1.5 rounded-full bg-white/10"><div className={`h-full rounded-full ${barClassName}`} style={{ width: `${agent.percentage}%` }} /></div></div>; }) : <p className="py-8 text-center text-sm text-slate-500">— Agent health not returned</p>}</div></section>;
}
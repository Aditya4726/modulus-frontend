export default function ProfileSettingsPage() { return <SettingsPanel title="Profile" description="Manage the identity used across your Modulus workspace." fields={["Display name", "Email address"]} />; }
function SettingsPanel({ title, description, fields }: { title: string; description: string; fields: string[] }) { return <div className="mx-auto max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.12em] text-blue-600">Settings</p><h1 className="mt-2 text-2xl font-bold text-slate-900">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p><div className="mt-8 rounded-xl border border-slate-200 bg-white p-6"><div className="space-y-5">{fields.map((field) => <label key={field} className="block"><span className="text-sm font-medium text-slate-700">{field}</span><input className="mt-2 w-full rounded-lg border border-slate-200 px-3 py-2.5 text-sm" placeholder="Not configured" /></label>)}</div><button className="mt-6 rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white hover:bg-blue-700">Save changes</button></div></div>; }

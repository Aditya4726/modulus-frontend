export default function NotificationsSettingsPage() { return <PreferencePage title="Notifications" description="Choose which reliability events should reach your team." options={["Incident detected", "Fix ready for approval", "Pipeline failed"]} />; }
function PreferencePage({ title, description, options }: { title: string; description: string; options: string[] }) { return <div className="mx-auto max-w-3xl"><p className="text-xs font-bold uppercase tracking-[0.12em] text-blue-600">Settings</p><h1 className="mt-2 text-2xl font-bold text-slate-900">{title}</h1><p className="mt-1 text-sm text-slate-500">{description}</p><div className="mt-8 divide-y divide-slate-100 rounded-xl border border-slate-200 bg-white">{options.map((option) => <label key={option} className="flex items-center justify-between p-5"><span className="text-sm font-medium text-slate-700">{option}</span><input type="checkbox" defaultChecked className="h-4 w-4 accent-blue-600" /></label>)}</div></div>; }
